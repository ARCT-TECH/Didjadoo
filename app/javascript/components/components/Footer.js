import React from "react";
import { BrowserRouter, NavLink } from "react-router-dom";

const Footer = (props) => {
  return (
    <NavLink to="/AboutUs">
      <div>AboutUs</div>
    </NavLink>
  );
};

export default Footer;
