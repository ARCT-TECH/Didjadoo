import React from "react";
import { BrowserRouter, NavLink } from "react-router-dom";

const Footer = (props) => {
  return (
    <NavLink to="/aboutus">
About Us
    </NavLink>
  );
};

export default Footer;
